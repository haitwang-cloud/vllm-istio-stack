apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: vllm-system
  labels:
    app: vllm
    service: vllm
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: vllm
  type: ClusterIP